#include <stdlib.h>
#include <stdio.h>
/* betty style doc for function main goes there */
/**
 * main - Entry point
 *
 * Description: 'get number'
 * Return: Always 0 (Success)
 */
int main(void)
{
        char c;
        c = 0;
        while(c <= 10)
        if(c < 10)
        {
            putchar(c);
            c++;
        }
        else if(c = 10)
        {
                c = 'a';
                while(c <= 'f')
                {
                        putchar(c);
                        c++;
                }
        }
        
        return (0);
}
